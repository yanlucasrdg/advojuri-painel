# URL do backend AdvoJuri (FastAPI). Ainda não deployado — ver README do
# backend (advojuri-api) para instruções de deploy. Enquanto isso, o painel
# funciona contra um backend local em localhost:8000.
VITE_API_BASE_URL=http://localhost:8000
